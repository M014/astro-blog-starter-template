---
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import { SITE_TITLE, SITE_DESCRIPTION } from '../consts';
---

<!doctype html>
<html lang="en">
	<head>
		<BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
		<style>
			/* 添加一点样式让代码块更好看 */
			main {
				margin: auto;
				padding: 1.5rem;
				max-width: 60ch;
			}
			pre {
				background-color: #f4f4f4; /* 浅灰色背景 */
				border: 1px solid #ddd;
				border-radius: 4px;
				padding: 1em;
				overflow-x: auto; /* 如果代码过长，可以横向滚动 */
				font-family: 'Courier New', Courier, monospace;
				font-size: 0.9em;
				white-space: pre-wrap;       /* 自动换行 */
    		word-wrap: break-word;     /* 强制断词 */
			}
			code {
				color: #333; /* 深灰色文字 */
			}
		</style>
	</head>
	<body>
		<Header />
		<main>
			<h1>Office Activation Commands</h1>
			<p>
				Below are the commands to activate Office Professional Plus 2016. Run these in an administrator command prompt.
			</p>
			
			<h2>Activation Script</h2>
			<pre><code>for /f %x in ('dir /b ..\root\Licenses16\proplusvl_kms*.xrm-ms') do cscript ospp.vbs /inslic:"..\root\Licenses16\%x"
for /f %x in ('dir /b ..\root\Licenses16\proplusvl_mak*.xrm-ms') do cscript ospp.vbs /inslic:"..\root\Licenses16\%x"
cscript ospp.vbs /inpkey:XQNVK-8JYDB-WJ9W3-YJ8YR-WFG99
cscript ospp.vbs /sethst:kms.jihuowin.com && cscript ospp.vbs /act</code></pre>

			<p>
				Please use these scripts responsibly and ensure you are compliant with software licensing agreements.
			</p>
		</main>
		<Footer />
	</body>
</html>
